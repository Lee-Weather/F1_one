# Exp1 实验笔记

## 实验索引

> **阶段目标**：验证 `czy/diff` 风格的有参考轨迹 X1 平地行走方案，解决无参考轨迹训练中出现的高频小碎步问题。
> **当前状态**：云平台 4090D 排队超 30 分钟未调度，已停止云端任务 TASK_20260817_070；改用远程服务器 10.12.201.5 训练（PID 2744613，run_name=exp1，14:37 启动，GPU 45%，ETA ~2.3h）。

| 编号 | 日期 | 摘要 | 状态 | Task ID | GM账号 | checkpoint |
| --- | --- | --- | --- | --- | --- | --- |
| exp1 | 2026-08-17 | 恢复 diff 有参考轨迹 PPO，改为按平面速度自适应步态周期，并将左右膝关节物理阻尼设为 8.0，待训练验证 | 训练中（远程） | TASK_20260817_070（已停） | limxmspwqm1g9hgfme@emalupe.com | 待训练 |

## 实验 exp1：恢复有参考轨迹的 X1 行走算法

### 1. 上一实验结果与教训

> 数据：exp0 无参考轨迹基线及其回放诊断。
> - Mean reward：120.78
> - Mean episode length：2265.51
> - 平均前进速度：0.588 m/s，指令速度约 0.5 m/s
> - 单腿步周期：约 0.122 s；估算步长：约 0.073 m；抬脚高度：约 0.016~0.020 m
>
> **核心教训**：
> - 仅依赖速度、接触和步态统计奖励时，策略能够获得较高训练奖励，但会收敛到高频小碎步局部最优。
> - 本轮需要使用显式相位和参考关节轨迹约束摆腿时序，不能继续依赖无参考轨迹的间接奖励组合。

### 2. 本轮修改目标

- 目标1：恢复 `czy/diff` 中基于相位生成 `ref_dof_pos` 的参考轨迹训练闭环。
- 目标2：让左右腿接触状态与参考 `stance_mask` 保持一致，形成稳定的交替步态。
- 目标3：验证膝关节物理阻尼设为 8.0 后，机器人姿态和摆腿稳定性是否改善。
- 目标4：将行走速度限制在 0~0.6 m/s，步态周期随命令平面速度变化；0.6 m/s 时达到最长 0.7s。
- 目标5：保留后退能力，后退命令速度不超过 0.4 m/s，并让速度奖励同时适用于前进、后退、侧向和斜向行走。
- 验收标准：本轮暂不设验收目标，训练完成后以回放诊断数据（步态周期、步长、抬脚高度、接触相位）为参考评估。

### 3. 修改内容

### 修改一：恢复 diff 有参考轨迹环境

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| 单帧 Actor 观测 | 49 | 47 | 使用相位 `sin/cos` 加三维速度命令 |
| 单帧 Critic 特权观测 | 57 | 73 | 增加 `q-ref_dof_pos`、`stance_mask` 等信息 |
| 步态周期 | 无显式关节参考 | 0.35~0.7s | 根据命令速度在线生成相位和参考轨迹 |
| `ref_joint_pos` | 无 | 2.2 | 约束当前关节位置跟踪参考关节位置 |
| `feet_contact_number` | 无 | 2.0 | 约束真实接触状态匹配参考支撑相位 |
| `use_ref_actions` | 无 | `False` | 默认使用参考奖励，不直接叠加参考动作 |

**理由**：参考轨迹提供明确的摆腿时序和关节姿态先验，降低策略通过高频小步获得速度奖励的可能性。

### 修改二：恢复 diff PPO 训练参数

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| learning_rate | `1e-3` | `1e-5` | 与参考轨迹方案保持一致 |
| entropy_coef | `0.01` | `0.001` | 减少后期无效探索 |
| num_learning_epochs | 5 | 2 | 配合较低学习率稳定更新 |
| max_iterations | 20000 | 3000 | 本轮按实验预算限制训练轮数 |

**理由**：恢复 `czy/diff` 中的训练超参数，避免用无参考轨迹版本的高学习率直接训练新观测结构。

### 修改三：提高左右膝关节物理阻尼

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| `left_knee_pitch_joint` 物理阻尼 | 1.5 | 8.0 | `initial_joint_damping` 第 4 项 |
| `right_knee_pitch_joint` 物理阻尼 | 1.5 | 8.0 | `initial_joint_damping` 第 10 项 |
| 控制器 PD damping | 10 | 10 | 保持不变，不与物理阻尼混淆 |

**理由**：提高膝关节对摆腿和落脚过程的物理阻尼，观察其对参考轨迹跟踪稳定性的影响。

### 修改四：按命令平面速度自适应 cycle_time

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| `cycle_speed_max` | 无 | 0.6 m/s | 行走速度上限 |
| `cycle_time_min` | 无 | 0.35s | 低速端周期下限 |
| `cycle_time_max` | 固定 0.7s | 0.7s | 0.6 m/s 时的最长周期 |

其中 `v_xy = sqrt(cmd_x^2 + cmd_y^2)`，先裁剪到 `0~0.6 m/s`，再按线性关系计算：`cycle_time = 0.35 + (v_xy / 0.6) * (0.7 - 0.35)`。纯站立时仍由站立逻辑将相位清零。

**理由**：按照当前机器人实际行走速度范围重新限定周期设计，保证 0.6 m/s 时周期不超过 0.7s；训练环境和 `sim2sim` 回放使用同一套映射和边界。

### 修改五：限制前后速度并修正平面速度奖励

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| 前向命令范围 | `[-0.4, 1.2]` | `[-0.4, 0.6]` | 保留后退，前进上限为 0.6 m/s |
| command curriculum | `max_curriculum=1.5` | 关闭 | 防止训练过程中重新扩展到 0.6 m/s 以上 |
| 全向命令 | 独立采样 x/y | 平面速度裁剪到 0.6 m/s | 使 `sqrt(cmd_x²+cmd_y²) ≤ 0.6` |
| `low_speed` 奖励 | 仅检查 x 方向 | 检查 xy 平面速度和方向 | 支持后退、侧向和斜向行走 |

`low_speed` 现在通过速度向量点积判断运动方向：点积为负时，表示实际运动方向与命令方向相反，因此后退命令不会被错误地当成方向错误。

### 4. 修改文件

- `humanoid/envs/x1/x1_dh_stand_env.py`：恢复独立相位、参考关节轨迹、参考关节奖励和接触相位奖励。
- `humanoid/envs/x1/x1_dh_stand_config.py`：恢复 diff 观测维度、奖励权重、PPO 参数，设置膝关节物理阻尼为 8.0，配置自适应周期边界，并限制前进/后退命令范围。
- `humanoid/envs/base/legged_robot.py`：使用已有的 `initial_joint_damping` 覆盖逻辑，使配置中的物理阻尼生效。
- `humanoid/scripts/sim2sim.py`：回放时按同一速度映射生成相位，避免训练和部署使用不同周期。

### 5. 训练参数

| 参数 | 值 |
| --- | --- |
| 训练方式 | 从零（远程服务器） |
| 远程主机 | 10.12.201.5，robot@，conda 环境 F1 |
| 远程目录 | ~/czy/exp1/exp_20260817_142919/F1_one |
| 训练 PID | 2744613（nohup，run_name=exp1） |
| 云端 Task ID | TASK_20260817_070（已停止，4090D 排队未调度） |
| max_iterations | 3000 |
| save_interval | 100 |
| num_envs | 4096 |
| seed | 5 |
| learning_rate | `1e-5` |
| 算力 | 远程服务器（A100 48G，单卡） |
| 代码 | 本地 scp 传输（不含 skills/、czy/data/、.git） |
| 启动命令 | `python humanoid/scripts/train.py --task=x1_dh_stand --run_name=exp1 --headless --max_iterations=3000` |

### 6. 预期与验收

> 本轮暂不设验收目标，以训练完成后回放诊断数据为参考。

> 训练完成后补充 `isaac_diag.csv` 中的步频、周期、步长、抬脚高度、接触占比和回放视频结果。

### 7. 实验结果

远程训练完成（iter 3000，约 2h），自适应周期回放完成（play_adaptive.py，60s 阶梯速度 0.2→0.6→0 m/s）。

#### 最终结果

| 指标 | 上一轮 | 实测 | 备注 |
| --- | --- | --- | --- |
| Mean reward | 120.78 | **172.88** | 无目标阈值，显著高于 exp0 |
| Mean episode length | 2265.51 | **2284.43** | 与 exp0 持平 |
| 单腿步周期 | 0.122 s | 见下方分段实测 | 周期随命令速度增长（自适应） |
| 估算步长 | 0.073 m | 见下方分段实测 | 0.6 m/s 时步长明显增大 |

**自适应周期回放（play_adaptive，阶梯命令 0.2→0.6 m/s）**：

| 段 | 命令 (m/s) | 实际速度 (m/s) | 理论周期 (s) | 实测触地间隔 L/R (s) | 备注 |
| --- | --- | --- | --- | --- | --- |
| 0 | 0.20 | 0.180 | 0.467 | 0.728 / 0.902 | 起步段 |
| 1 | 0.40 | 0.339 | 0.583 | 1.169 / 1.185 | 周期增大 |
| 2 | 0.60 | 0.511 | 0.700 | 1.208 / 1.370 | 周期最长 |
| 3 | 0.40 | 0.478 | 0.583 | 0.936 / 0.706 | 降速 |
| 4 | 0.20 | 0.213 | 0.467 | 0.591 / 0.929 | 继续降速 |
| 5 | 0.00 | -0.006 | 0.350 | 站立（无触地切换） | 站立 |

> 说明：实测触地间隔基于 `isaac_diag.csv` 脚接触力上升沿（阈值 1.0N），含双支撑相位，数值略大于纯相位理论周期；但**随命令速度单调增长的趋势与自适应周期映射一致**，证明步态周期随速度自适应。
> 视频：`czy/data/exp1/play_output.mp4`（画面叠加 CMD/REAL/CYCLE 与双脚着地状态）；数据：`czy/data/exp1/isaac_diag.csv`；模型：`czy/data/exp1/model.pt`。

#### 训练趋势

| iter | Mean reward | Mean episode length |
| --- | --- | --- |
| 206 | 35.72 | 965.20 |
| 1110 | 133.44 | 2188.77 |
| 1694 | 158.91 | 2299.73 |
| 2770 | 167.48 | 2267.58 |
| 3000 | 172.88 | 2284.43 |

#### 各奖励项最终值

| 奖励项 | 权重 | 最终值 | 说明 |
| --- | --- | --- | --- |
| `ref_joint_pos` | 2.2 | 1.3308 | 参考关节位置跟踪（主要贡献项） |
| `feet_contact_number` | 2.0 | 1.6959 | 参考支撑相位接触 |
| `tracking_lin_vel` | 1.8 | 1.2546 | 线速度跟踪 |
| `tracking_ang_vel` | 1.1 | 0.8208 | 角速度跟踪 |

**结论**：训练收敛（reward 172.88 远超 exp0 的 120.78，全程稳定上升）；回放验证 0.2/0.4/0.6 m/s 三个速度档均能跟踪前向命令（实际 0.18/0.34/0.51 m/s），**步态周期随命令速度单调增长（触地间隔 0.73s→1.17s→1.29s），自适应周期机制生效**；站立段正常静止。但存在**严重右偏问题**：机身朝向近 0° 时轨迹方向恒为 -31°~-35°（螃蟹步），体坐标 vy 恒 -0.11~-0.18 m/s；0.6 m/s 段甩脚（脚 yaw 外八 34°~42°）+ 航向漂移 -43°/10s，最终 32.1s 摔倒触发 reset。

**根因分析**：
1. **侧滑无奖励压力**：`tracking_lin_vel`（权重 1.8，σ=5）对 vy=0.17 的侧滑仅损失 ~0.25 总奖励（172 中占比 0.15%），策略无动机修正；actor 观测（47 维单帧）也不含实际线速度（仅 critic 特权观测含 `base_lin_vel`）。
2. **`feet_rotation` 奖励失效**：实现只罚脚 roll/pitch（`feet_euler_xyz[:,:,:2]`），不罚 yaw 外八；且 `exp(-15·Σr²)` 在正常行走时饱和，训练日志恒 0.0000，形同虚设。脚外八（右脚 > 左脚）不对称持续把身体推向右侧。
3. 高速段（0.6 m/s 为训练命令上限）叠加侧滑+甩脚 → 失稳摔倒。

**下一轮（exp1.1）修改计划**——`tracking_sigma` 保持 5 不变，目标：消除螃蟹步（vy 偏差 < 0.03 m/s、路径方向与机身夹角 < 5°），0.6 m/s 不摔：
1. **新增侧滑专项奖励 `lateral_vel`（核心手段）**：`r = exp(-|base_lin_vel_y|·k)`，k=20，权重 0.6。直接以 vy 为目标，vy=0.17 时该项奖励仅 0.03（满值 1，损失 0.57 总奖励，是原 tracking_lin_vel 惩罚力度的 30 倍）；且 exp 峰值在 vy=0 处梯度最大，站立/行走/全速度段通用。
2. **新增脚 yaw 外八奖励 `feet_yaw_align`**：基于 `feet_quat` 四元数手写旋转取脚前向轴（规避 euler 万向锁伪影与 GM 镜像 quat_rotate bug），`r = exp(-k·Σ|foot_yaw_rel|)`，k=10，权重 0.4；修复原 `feet_rotation` 只罚 roll/pitch 且饱和恒 0 的缺陷，从步态源头抑制甩脚外八。
3. **重写 `_reward_feet_rotation`（原项修复）**：保留 roll/pitch 惩罚但去饱和（15→5），使该项在脚倾斜时真正出力。
4. 原有 `tracking_lin_vel`/`tracking_sigma`/观测架构/网络结构全部不动，避免影响已收敛的前向跟踪与部署链路。
5. 训练配置不变（3000 iter，10.12.201.5，run_name=exp1.1）。
6. **验证**：play_adaptive 回放 + analyze_diag/analyze_drift——vy bias、路径-机身夹角、脚 yaw 外八、0.6 段无 reset，全量对照 exp1 表格记录。

### 8. exp1.1 启动记录（2026-08-17 18:30）

| 参数 | 值 |
| --- | --- |
| 代码提交 | `5c6ca03`（含 lateral_vel/feet_yaw_align 奖励、feet_rotation 去饱和） |
| 远程目录 | ~/czy/exp1/exp_20260817_183000/F1_one（git archive 干净部署，无 skills/czy/data） |
| 训练 PID | 2804864（nohup，run_name=exp1.1，max_iterations=3000） |
| 启动验证 | 进程存活，GPU 8948MiB/41%，日志含 rew_lateral_vel / rew_feet_yaw_align 项 ✅ |
| 预计耗时 | ~2.3h（ETA 8443s） |

#### exp1.1 结果（2026-08-18 完成，iter 3000，Mean reward 150.20）

**核心指标对比 exp1 → exp1.1**：

| 指标 | exp1 | exp1.1 | 目标 | 判定 |
| --- | --- | --- | --- | --- |
| 总位移方向（世界系） | **-34.3°（右偏螃蟹步）** | **+4.2°** | — | ✅ 根治 |
| 体坐标 vy bias（主力段 0.4/0.6） | -0.17 / -0.18 | **-0.029 / -0.004** | <0.03 | ✅ 达标 |
| 机身 yaw 漂移（0.6 段） | -43°/10s | 稳定在 +5~+17° | — | ✅ 大幅改善 |
| 脚 yaw 外八（0.6 段 L/R） | +34°/+42° | -18°/-10° | — | ✅ 减半且对称 |
| 0.6 m/s 段完整性 | 32.1s 摔倒 | **完整走完**，摔倒推迟至 36s（降速段） | 不摔 | ⚠️ 部分达标 |
| 前向速度跟踪（0.4 段） | 0.339 | 0.264 | — | ⚠️ 下降 ~22% |
| Mean reward | 172.88 | 150.20 | — | 侧滑惩罚的代价，正常 |

**结论**：`lateral_vel`+`feet_yaw_align` 双奖励方案**成功消除螃蟹步**——机器人从"斜着走"（-34°）变为基本直线（+4°），侧滑速度从 0.17 降到 <0.03 m/s，脚外八减半。代价：前向速度跟踪下降 10~22%（0.4 段 0.34→0.26），起步段（seg0）有 +0.08 左偏瞬态；降速段（seg3，36s）仍摔一次。

**数据归档**：`czy/data/exp1.1/`（model.pt / play_output.mp4 / isaac_diag.csv）。

**下一轮方向（exp1.2）**——基于 exp1.1 深度数据分析（analyze_exp11_fail.py，含时基勘误 dt=0.01s/100Hz，实测步周期与理论自适应周期吻合 97%~101%，步频调度正确）：

三个问题的根因与对策：
1. **降速摔倒 = 训练分布缺失（核心发现）**：`episode_length_s=24` < `resampling_time=25`，每个 episode 命令只采样一次、**全程无变速**；回放 0.6→0.4 阶跃时策略失控（vx 反升 0.62→3.78 前扑，步频崩坏至 0.12s/步，扭矩 RMS 翻 2.4 倍）。→ 对策：`resampling_time` 25→10s，每 episode 含 2~3 次变速。
2. **前向跟踪下降 = 不敢走快**：步频 100% 跟调度、步长同比缩水（seg1 步长比 0.68 ↔ 速度亏 31%），合成速度仅比 vx 大 2~3%（排除斜走补偿）；扭矩 RMS 恒 ~15Nm（排除僵硬步态）。→ 对策：`lateral_vel` 权重 0.6→0.4，`track_vel_hard` 0.5→1.0。
3. **冷启动左偏锁死**：0.15s 内 vy +0.54，1s 内航向锁 +7° 整段不收敛（段内左移 0.48m）；热启动同命令正常。→ 重采样加快后自然覆盖起步-加速场景，先观察。
4. `_get_cycle_time` 对命令速度加 EMA 平滑（τ≈0.5s），变速时周期连续收缩、相位不断裂。
5. `max_iterations` 3000→5000（reward 150 仍在上升）。
6. 低优先：落地冲击 3~4 倍体重（峰值 2.4~3.3kN），必要时加强 `feet_contact_forces` 惩罚。
